// import cookieparser from 'cookieparser'

export const action = {
    // 服务端渲染每次从cookie中去用户信息存入vuex
    //async nuxtServerInit( { commit }, { req }) {
        // let sendinfoUser = null
        // console.log(req.headers.cookie)
        // if(req.headers.cookie){
        //     const cookieParser = cookieparser.parse(req.headers.cookie)
        //     sendinfoUser = (cookieParser.sendinfoUser && JSON.parse(cookieParser.sendinfoUser)) || null
        //     console.log(sendinfoUser)
        // }
        //
        // commit('auth/setUser', sendinfoUser)
    //}
}